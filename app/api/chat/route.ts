import { NextResponse } from "next/server";

// Placeholder until the agent (Session C) is built.
export async function POST() {
  return NextResponse.json({
    reply: "Rawr! I'm Dino. My travel brain arrives in the next build step. For now you can use the shop tab.",
  });
}
